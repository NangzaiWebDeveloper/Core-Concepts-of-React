import React from 'react';
import Menu from '../componentRoutes/Menu';

const Document = () => {
    return (
        <div>
            <Menu/>
            This is Document
        </div>
    );
};

export default Document;