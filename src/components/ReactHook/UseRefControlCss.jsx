import React from 'react';

const UseRefControlCss = () => {
    return (
        <div>

            CSS effects not show properly that's why cannot write code.

        </div>
    );
};

export default UseRefControlCss;